apiVersion: v1
kind: Service
metadata:
  name: kalink-studio
  labels:
    app: kalink-studio
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: kalink-studio
